# secrets
.env
.mcp.json

# python envs
.venv/
.venv-ml/
__pycache__/
*.pyc

# large lossless masters (re-render from the .py; mp3s are committed for playback)
*.wav

# third-party copyrighted MIDIs used only for private study — do not redistribute
midi/

# visual inspiration seeds (others' images) — kept local, credited in creations.json
inspiration/

# bundled MCP tools (clone locally; not part of this repo)
tools/

# os / scratch
.DS_Store
*.output
